namespace Webium.Core
{
    /// <summary>
    /// Selects which render backend a <c>WebiumSurface</c> uses.
    /// </summary>
    public enum RenderBackendType : byte
    {
        UGUI = 0,
        UIElements = 1
    }
}
